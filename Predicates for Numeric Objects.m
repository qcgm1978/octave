##abs returns the absolute value of the input if it is a real number, and the length of the input if it is a complex number. function a = abs (x)      if (isreal (x))          a = sign (x) .* x;        elseif (iscomplex (x))          a = sqrt (real(x).^2 + imag(x).^2);        endifendfunctionassert(abs(1),1)assert(abs(-1),1)##assert(abs(complex(2,3)),3.6056)##Return true if x is a numeric object, i.e., an integer, real, or complex array.assert(isnumeric(1),true)assert(isnumeric(complex(1)),true)assert(isnumeric(complex(1,2)),true)##Logical and character arrays are not considered to be numeric.assert(isnumeric(true),false)assert(isnumeric("char"),false)##Return true if x is a logical object.assert(isbool(true),islogical(true))##Objects of class double or single are floating-point objects.assert(isfloat(1.2),isfloat(single(1)))##Return true if x is a non-complex matrix or scalar.assert(isreal(1.),!isreal(complex(1)))assert(isreal([1;2]),isreal([complex(1)]))##Return true if x is a complex-valued numeric object.assert(!iscomplex(1),iscomplex(complex(1)))assert(ismatrix([]),ismatrix(1:3),"Return true if a is a 2-D array, i.e., size (a) returns [M N] with non-negative M and N.")assert(isvector(ones(1,1)),isvector(1)," As a consequence a 1x1 array, or scalar, is also a vector.")assert(isrow(zeros(1,5)),true,"Return true if x is a row vector, i.e., size (x) returns [1 N] with non-negative N.")assert(issquare(zeros(5,5)),true,"Return true if x is a square matrix, i.e., size (x) returns [N N] with non-negative N.")assert(issymmetric(ones(2,2)),"Return true if A is a symmetric or skew-symmetric matrix within the tolerance spec- ified by tol.")assert(issymmetric(ones(2,2),0),"The default tolerance is zero (uses faster code).")assert(issymmetric(ones(2,2),"nonskew"),'The type of symmetry to check may be specified with the additional input "nonskew"')assert(issymmetric(ones(2,2),"skew"),'"skew" for skew-symmetry')##A matrix is symmetric if the transpose of the matrix is equal to the original matrix: A == A.'assert(transpose(ones(2,2)),ones(2,2))##A matrix is skew-symmetric if the transpose of the matrix is equal to the negative of the original matrix: A == -A.'assert(transpose(zeros(2,2)),-zeros(2,2))assert(ishermitian(ones(2,2)),"Return true if A is a Hermitian or skew-Hermitian matrix within the tolerance spec- ified by tol.")assert(isdefinite(ones(2,2),0),-1)##B为n阶矩阵，E为单位矩阵，a为正实数。aE+B在a充分大时，aE+B为正定矩阵。（B必须为对称阵）B=ones(3,3);E=[1,0,0;0,1,0;0,0,1];a=1000;isdefinite(a*E+B);assert(isdefinite(a*E+B),1)assert(!isbanded(ones(2,2),3,0),"Return true if A is a matrix with entries confined between lower diagonals below the main diagonal and upper diagonals above the main diagonal.")